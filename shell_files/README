Compile with the command "make". To run type "./run" The compiled files can be removed using "make clean".
Everything seems to be working properly. There are memory leaks that we cannot seem to nail down.

